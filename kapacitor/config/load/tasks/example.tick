dbrp "telegraf"."autogen"

stream
    |from()
        .measurement('mock')
        .groupBy(*)
    |alert()
        .warn(lambda: "constant" < 5)
        .crit(lambda: "constant" > 5)
        // Send alerts to the `cpu` topic
        .topic('mock')
